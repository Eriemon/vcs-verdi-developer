interface:
  display_name: "VCS Verdi Developer"
  short_description: "Guide scripted non-GUI VCS simulation, Makefile/Edalize/cocotb import, DPI, FSDB/NPI, coverage, evidence, and Verdi checks."
  default_prompt: "Use $vcs-verdi-developer to validate a scripted non-GUI VCS simulation, Makefile, Edalize, or cocotb import, DPI dry-run, FSDB/NPI readback, evidence, and regression gate."
